import {useEffect} from "react";import {Platform} from "react-native";import {router} from "expo-router";import * as Device from "expo-device";import * as Notifications from "expo-notifications";import * as SecureStore from "expo-secure-store";import {api} from "./api";import type {UserRole} from "./types";
const PUSH_TOKEN_KEY="nitido.push-token.v1";
export async function registerPush(){if(!Device.isDevice)throw new Error("Notificările push necesită un dispozitiv fizic.");const permission=await Notifications.requestPermissionsAsync();if(permission.status!=="granted")return false;const nativeToken=await Notifications.getDevicePushTokenAsync();const token=String(nativeToken.data);await api("/api/push/register",{method:"POST",body:JSON.stringify({platform:Platform.OS==="ios"?"IOS":"ANDROID",deviceToken:token})});await SecureStore.setItemAsync(PUSH_TOKEN_KEY,token);return true}
export async function unregisterCurrentPush(){const token=await SecureStore.getItemAsync(PUSH_TOKEN_KEY);if(!token)return;try{await api("/api/push/unregister",{method:"POST",body:JSON.stringify({deviceToken:token})})}finally{await SecureStore.deleteItemAsync(PUSH_TOKEN_KEY)}}
export function useNotificationRouting(role:UserRole|null){useEffect(()=>Notifications.addNotificationResponseReceivedListener(response=>{const data=response.notification.request.content.data??{};const event=typeof data.event_type==="string"?data.event_type:"";if(!role)return;if(role==="firma"&&event==="JOB_CREATED")router.push("/(firma)");else if(role==="client"&&["JOB_ACCEPTED","JOB_ARRIVED","JOB_COMPLETED"].includes(event))router.push("/(client)/jobs");}),[role])}
