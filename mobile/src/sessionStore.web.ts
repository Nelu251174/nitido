export const nativeSessionRequired=false;
export async function getSessionToken(){return null}
export async function setSessionToken(token:string|null){void token;/* Web authentication remains in the backend httpOnly cookie. */}
