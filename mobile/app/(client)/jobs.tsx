import {router} from "expo-router";import {useState} from "react";import {Pressable,StyleSheet,Text,View} from "react-native";import {useJobs} from "@/useJobs";import {JobCard} from "@/jobUi";import {AppScreen,EmptyState,InlineState} from "@/mobileUi";import {colors} from "@/theme";const FILTERS=["Toate","Active","Finalizate"] as const;
export default function Jobs(){const [filter,setFilter]=useState<typeof FILTERS[number]>("Toate");const {jobs,loading,error,reload}=useJobs();const shown=jobs.filter(j=>filter==="Toate"||filter==="Active"&&["waiting","accepted","arrived"].includes(j.status)||filter==="Finalizate"&&["completed","cancelled","no_show"].includes(j.status));return <AppScreen eyebrow="ISTORIC PERSONAL" title="Lucrările mele" subtitle="Statusurile sunt cele confirmate de sistemul NITIDO."><View style={s.filters}>{FILTERS.map(item=><Pressable accessibilityRole="button" key={item} onPress={()=>setFilter(item)} style={[s.filter,filter===item&&s.active]}><Text style={[s.filterText,filter===item&&s.activeText]}>{item}</Text></Pressable>)}</View><InlineState loading={loading} error={error} onRetry={()=>void reload()}/>{!loading&&!error&&(shown.length?shown.map(job=><JobCard key={job.id} job={job} onPress={()=>router.push({pathname:"/(client)/job/[id]",params:{id:job.id}})}/>):<EmptyState icon="briefcase-outline" title="Nu există lucrări în această secțiune" body="Poți publica o lucrare nouă oricând." cta="Postează" onPress={()=>router.push("/(client)/post")}/>)}</AppScreen>}
const s=StyleSheet.create({filters:{flexDirection:"row",padding:4,borderRadius:15,backgroundColor:"#E9E9E3"},filter:{flex:1,minHeight:40,borderRadius:12,alignItems:"center",justifyContent:"center"},active:{backgroundColor:colors.white},filterText:{fontSize:12,fontWeight:"700",color:colors.muted},activeText:{color:colors.greenDark}});
