import {useState} from "react";import {TextInput} from "react-native";import {AppScreen,EmptyState,PrimaryButton} from "@/mobileUi";import {colors} from "@/theme";export default function Forgot(){const [email,setEmail]=useState("");const [requested,setRequested]=useState(false);return <AppScreen eyebrow="ACCES CONT" title="Ai uitat parola?" subtitle="Introdu emailul contului pentru a vedea opțiunile disponibile.">{requested?<EmptyState icon="headset-outline" title="Recuperare asistată" body="Recuperarea automată nu este încă disponibilă în API. Contactează echipa NITIDO la 0341.402.403 sau contact@nitido.ro."/>:<><TextInput accessibilityLabel="Email" autoCapitalize="none" keyboardType="email-address" placeholder="nume@email.ro" placeholderTextColor="#929A95" value={email} onChangeText={setEmail} style={{minHeight:56,borderRadius:16,borderWidth:1,borderColor:colors.border,backgroundColor:colors.white,paddingHorizontal:17,fontSize:16}}/><PrimaryButton disabled={!email.includes("@") } title="Vezi opțiunile" onPress={()=>setRequested(true)}/></>}</AppScreen>}
