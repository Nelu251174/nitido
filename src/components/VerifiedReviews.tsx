type PublicReview={id:string;firmName:string;rating:number;reviewText:string|null;reviewer:string;badge:string;createdAt:string};
export function VerifiedReviews({reviews}:{reviews:PublicReview[]}){
  return <section className="v2-container py-20"><div className="v2-eyebrow">RECENZII VERIFICATE</div><h2 className="v2-h2 mt-4">Ce spun clienții după lucrări reale</h2>{reviews.length===0?<div className="mt-9 rounded-[22px] border border-[#d8d7d0] bg-white p-8 text-[#4d5751]">Recenziile clienților vor apărea aici după primele lucrări finalizate prin NITIDO.</div>:<div className="mt-9 grid grid-cols-3 gap-5 max-[1000px]:grid-cols-1">{reviews.slice(0,6).map(review=><article key={review.id} className="rounded-[22px] border border-[#d8d7d0] bg-white p-7"><div className="flex items-center justify-between gap-3"><span className="text-lg tracking-[.15em] text-[#1b8a4c]" aria-label={`${review.rating} din 5 stele`}>{"★".repeat(review.rating)}<span className="text-[#d8d7d0]">{"★".repeat(5-review.rating)}</span></span><span className="rounded-full bg-[#e4f0e8] px-3 py-1 text-[10px] font-bold text-[#14663a]">{review.badge}</span></div>{review.reviewText&&<p className="mt-5 leading-7 text-[#3e4842]">„{review.reviewText}”</p>}<div className="mt-6 border-t border-[#e3e2da] pt-4 text-sm"><b>{review.reviewer}</b><span className="block text-[#6b756f]">Client verificat · {review.firmName}</span></div></article>)}</div>}</section>;
}
